# Birthday Reminder

Small Express app to store birthdays and optionally send daily email reminders via SMTP.

Quick start

1. Install dependencies

```bash
npm install
```

2. Copy `.env.example` to `.env` and set values (especially SMTP\_\* if you want emails)

3. Run

```bash
npm start
```

Visit http://localhost:3000 to manage birthdays.
